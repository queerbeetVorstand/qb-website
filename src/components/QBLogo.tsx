import React, { ReactElement } from "react";
import { Box, Typography } from "@material-ui/core";

export default function QBLogo(): ReactElement {
  return (
    <Box m={2}>
      <Typography color="primary">Queerbeet</Typography>
    </Box>
  );
}
